# src/features

Boundary: feature slices.

Suggested slices:
- `features/screenshot/*`
- `features/recording/*`

Rules:
- Feature modules can include UI + local state + small helpers.
- Shared helpers belong in `src/utils`.
